# Agentic RAG

An agentic RAG system can plan retrieval before answering. The agent can choose collections, compile sparse query terms, inspect document hits, and rerun a narrower search.

Logging the query plan makes retrieval behavior easier to test.
