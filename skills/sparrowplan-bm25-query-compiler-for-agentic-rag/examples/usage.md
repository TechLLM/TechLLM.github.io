# Example Usage

```bash
python scripts/run.py --question-file examples/question.txt --corpus examples/corpus
```

The command prints JSON with the compiled weighted BM25 query plan and staged retrieval results.
